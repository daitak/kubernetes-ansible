ansible_ssh_user: root
kube_service_addresses: 11.254.0.0/16 # MUST be defined as a range not used in your infrastructure
kube_overlay_ip: 11.0.0.0 # defined if using flannel as the overlay network
kube_overlay_prefix: 16 # defined if using flannel as the overlay network
